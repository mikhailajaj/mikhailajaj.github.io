# dependencies
node_modules/
.pnp
.pnp.*

# build output
.next/
out/
build/
dist/

# generated by `next dev` / `next build`
next-env.d.ts
*.tsbuildinfo

# environment
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editor / OS
.DS_Store
.vscode/
.idea/

# NOTE: assets-src/ is intentionally NOT ignored — it holds the full-resolution
# image originals whose optimised WebP derivatives live in public/. Losing them
# on a fresh clone would make the images unreproducible.
